import { Repository } from 'typeorm';
import { Domain } from './entities/domain.entity.js';
import { Subdomain } from './entities/subdomain.entity.js';
import { CreateDomainDto } from './dto/create-domain.dto.js';
import { UpdateDomainDto } from './dto/update-domain.dto.js';
import { CreateSubdomainDto } from './dto/create-subdomain.dto.js';
import { VhostService } from './services/vhost.service.js';
import { PhpFpmService } from './services/php-fpm.service.js';
import { SystemUsersService } from '../system-users/system-users.service.js';
import { TransactionManagerService } from '../../core/rollback/transaction-manager.service.js';
import { InputValidatorService } from '../../core/validators/input-validator.service.js';
import { AuditLoggerService } from '../../core/audit/audit-logger.service.js';
import { PathResolverService } from '../../core/distro/path-resolver.service.js';
import { LoggerService } from '../../common/logger/logger.service.js';
export declare class DomainsService {
    private readonly domainRepository;
    private readonly subdomainRepository;
    private readonly vhostService;
    private readonly phpFpmService;
    private readonly systemUsersService;
    private readonly transactionManager;
    private readonly inputValidator;
    private readonly auditLogger;
    private readonly pathResolver;
    private readonly logger;
    constructor(domainRepository: Repository<Domain>, subdomainRepository: Repository<Subdomain>, vhostService: VhostService, phpFpmService: PhpFpmService, systemUsersService: SystemUsersService, transactionManager: TransactionManagerService, inputValidator: InputValidatorService, auditLogger: AuditLoggerService, pathResolver: PathResolverService, logger: LoggerService);
    create(dto: CreateDomainDto, performedBy?: string): Promise<Domain>;
    findAll(ownerId?: string): Promise<Domain[]>;
    findOne(id: string): Promise<Domain>;
    findByName(name: string): Promise<Domain | null>;
    update(id: string, dto: UpdateDomainDto, performedBy?: string): Promise<Domain>;
    delete(id: string, performedBy?: string): Promise<void>;
    suspend(id: string, performedBy?: string): Promise<Domain>;
    unsuspend(id: string, performedBy?: string): Promise<Domain>;
    getStats(id: string): Promise<{
        diskUsageMb: number;
        bandwidthUsedMb: number;
        subdomainCount: number;
    }>;
    createSubdomain(dto: CreateSubdomainDto, _performedBy?: string): Promise<Subdomain>;
    listSubdomains(domainId: string): Promise<Subdomain[]>;
    deleteSubdomain(id: string): Promise<void>;
    private generateUsername;
    private regenerateVhost;
}

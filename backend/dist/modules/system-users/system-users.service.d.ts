import { Repository } from 'typeorm';
import { SystemUser, SystemUserStatus } from './entities/system-user.entity.js';
import { SSHKey } from './entities/ssh-key.entity.js';
import { CreateSystemUserDto } from './dto/create-system-user.dto.js';
import { UpdateSystemUserDto } from './dto/update-system-user.dto.js';
import { AddSSHKeyDto } from './dto/add-ssh-key.dto.js';
import { CommandExecutorService } from '../../core/executor/command-executor.service.js';
import { PathResolverService } from '../../core/distro/path-resolver.service.js';
import { TransactionManagerService } from '../../core/rollback/transaction-manager.service.js';
import { InputValidatorService } from '../../core/validators/input-validator.service.js';
import { AuditLoggerService } from '../../core/audit/audit-logger.service.js';
import { LoggerService } from '../../common/logger/logger.service.js';
export interface SystemUserWithSSHKeys {
    id: string;
    username: string;
    uid: number;
    gid: number;
    homeDirectory: string;
    shell: string;
    status: SystemUserStatus;
    diskQuotaMb: number;
    diskUsedMb: number;
    inodeQuota: number;
    inodeUsed: number;
    sshEnabled: boolean;
    sftpOnly: boolean;
    ownerId?: string;
    createdAt: Date;
    updatedAt: Date;
    deletedAt?: Date | null;
    sshKeys?: SSHKey[];
}
export declare class SystemUsersService {
    private readonly systemUserRepository;
    private readonly sshKeyRepository;
    private readonly commandExecutor;
    private readonly pathResolver;
    private readonly transactionManager;
    private readonly inputValidator;
    private readonly auditLogger;
    private readonly logger;
    private readonly MIN_UID;
    private readonly MAX_UID;
    constructor(systemUserRepository: Repository<SystemUser>, sshKeyRepository: Repository<SSHKey>, commandExecutor: CommandExecutorService, pathResolver: PathResolverService, transactionManager: TransactionManagerService, inputValidator: InputValidatorService, auditLogger: AuditLoggerService, logger: LoggerService);
    create(dto: CreateSystemUserDto, performedBy?: string): Promise<SystemUser>;
    findAll(ownerId?: string): Promise<SystemUser[]>;
    findOne(id: string): Promise<SystemUserWithSSHKeys>;
    findByUsername(username: string): Promise<SystemUser | null>;
    update(id: string, dto: UpdateSystemUserDto, performedBy?: string): Promise<SystemUser>;
    delete(id: string, performedBy?: string): Promise<void>;
    setPassword(id: string, password: string, performedBy?: string): Promise<void>;
    getQuotaUsage(id: string): Promise<{
        diskUsedMb: number;
        inodeUsed: number;
    }>;
    addSSHKey(systemUserId: string, dto: AddSSHKeyDto, performedBy?: string): Promise<SSHKey>;
    removeSSHKey(systemUserId: string, keyId: string, performedBy?: string): Promise<void>;
    listSSHKeys(systemUserId: string): Promise<SSHKey[]>;
    private findNextAvailableUid;
    private setPasswordInternal;
    private createDirectoryStructure;
    private generateDefaultIndexHtml;
    private setQuotaInternal;
    private suspendUserInternal;
    private unsuspendUserInternal;
    private syncAuthorizedKeys;
    private parseSSHPublicKey;
    private estimateRSAKeyBits;
}
